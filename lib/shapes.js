const textColor = require('./cli');
const shapeColor = require('./cli');

class Shapes {
    constructor() {
        this.width = width;
        this.height = height;
        this.fill = fill;
        this.stroke = stroke;
    }
    render(width, height) {
        
    }

}